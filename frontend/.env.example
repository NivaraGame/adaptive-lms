# API Configuration
# Backend API base URL - used for direct API calls (not proxied)
# In development, the Vite proxy handles /api/* requests
# In production, this should point to your deployed backend
VITE_API_BASE_URL=http://localhost:8000
